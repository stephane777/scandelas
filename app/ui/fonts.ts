import { Inter, Montserrat, Roboto_Condensed } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const monserrat = Montserrat({ subsets: ['latin'], weight: ['400', '800'] });
export const roboto = Roboto_Condensed({
  subsets: ['latin'],
  weight: ['400', '700'],
});
